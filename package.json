{
  "name": "enhanceconf-website",
  "version": "1.0.0",
  "description": "",
  "main": "spec.js",
  "scripts": {
    "start": "speclate --debug 5006",
    "build": "speclate --all",
    "dev-watch": "nodemon --watch pages --watch components --watch pages/layout.html --watch styles --watch spec.js -e html,js,json  --watch speclate.js --exec npm run build",

    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "speclate": "7.x.x"
  }
}
